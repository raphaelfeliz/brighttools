# Seed Backlog

Place raw ideas, bug reports, and features here as unorganized files (e.g. `YYYY-MM-DD-slug.md`). 

Agents process these files using the `/create-work-items` workflow and move them to `seeds/created/` after scaffolding the corresponding work items.
